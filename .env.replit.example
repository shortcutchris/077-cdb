# Replit Environment Variables Example
# Copy these to Replit Secrets (not .env file for security)

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI Configuration
VITE_OPENAI_API_KEY=your_openai_api_key

# Optional: GitHub OAuth (if using server-side auth)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Production URL (Replit will provide this)
# WICHTIG: Diese URL MUSS gesetzt werden für GitHub OAuth!
# Ersetze mit deiner tatsächlichen Replit URL
VITE_APP_URL=https://your-repl-name.your-username.repl.co

# CORS Configuration
# Add your Replit URL to Supabase allowed URLs in Dashboard:
# 1. Go to Supabase Dashboard > Authentication > URL Configuration
# 2. Add your Replit URLs to:
#    - Site URL: https://your-repl-name.your-username.repl.co
#    - Redirect URLs: 
#      - https://your-repl-name.your-username.repl.co/*
#      - https://*.repl.co/*
#      - https://*.replit.dev/*

# Optional: Different Supabase URL for Replit (if using proxy)
VITE_REPLIT_SUPABASE_URL=